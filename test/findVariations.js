const assert = require('assert');
const { findVariations } = require('../utils/find-variations')

describe('findVariations', () => {
  describe('Check known exact match', () => {
    //TODO tests    
    assert.ok(!!findVariations);
  });
});
